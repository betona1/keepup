# KeepUp — 마스터 기획서

> **한 줄 정의**: 습관을 사진으로 인증하면 도장이 찍히고, 마감 3시간 전 알림이 놓치지 않게 붙잡아 주는
> **네이티브 습관 앱**. 63일 뒤엔 도장으로 가득 찬 기록만 남는다.
>
> 문서 표기: **[MVP]** 지금 만드는 것 · **[선택]** 여건 되면 · **[향후]** 백엔드/확장 필요.

---

## 0. 제품 생태계 한눈에

```
┌─────────────────────────────┐        ┌──────────────────────────────────────┐
│         KeepUp 앱            │        │            keywordream 웹              │
│   (네이티브 Flutter, PWA 아님) │        │   (Cloudflare Workers + D1 + KV + R2)  │
│                             │        │                                        │
│  · 적립형 / 결과형 루틴        │  다운로드 │  keywordream.com   → 브랜드 메인          │
│  · 사진 인증 + 날짜 워터마크    │ ◀──────  │     · 간편로그인(구글/네이버/카카오/이메일) │
│  · 마감 3시간 전 로컬 알림     │        │     · 회원관리(마이페이지/관리자)          │
│  · 공유(카톡 등)              │  성과공유 │  keepup.keywordream.com → 앱 컴패니언   │
│  · 기록·추억 앨범             │ ──────▶  │     · 홍보 홈 / 법적 / 문의              │
│    (앱은 계정 없음)            │  딥링크   │     · 성과 인증 게시판(로그인 후 작성)     │
└─────────────────────────────┘        └──────────────────────────────────────┘
        앱 = 주인공                              웹 = 홍보 · 성과 자랑 · 회원
```

핵심 원칙: **앱이 본연의 기능에 충실**하고(혼자 써도 완결되는 자기만족용, 계정 없음), **웹은 홍보·성과 자랑·회원관리**를 맡는다. 웹 성과 게시판에 올릴 때만 웹 계정으로 로그인한다. 앱과 웹은 별도 폴더/저장소로 분리한다(`E:\app\keepup` = 앱, `E:\app\keywordream_web` = 웹 2종).

---

## 1. 배경 & 사용자

- **출발점**: 카카오톡 오픈채팅 습관 챌린지 모임. 매일 인증하고 반장이 구글시트에 기록하던 운영 방식에서 출발.
- **방향 전환**: 남을 관리·정산하는 다인 운영이 아니라, **혼자 쓰는 자기만족용**이 1순위. 그래서 앱은 계정·서버 없이 폰 안에서 완결.
- **주 사용자**: 스스로 습관을 만들고 싶은 개인. "미루면 사라지는 습관"을 강제로 붙잡고, 지나온 기록을 추억하고 싶은 사람.

---

## 2. 앱 — 챌린지 유형 (핵심 규칙)

원래 모임의 두 가지 실행 방식을 그대로 앱의 루틴 유형으로 가져온다.

### 2.1 적립형 (매일 쌓기) [MVP]
- 선언한 루틴을 **매일 실행**하고 인증하며 쌓는다.
- 루틴 구성: **메인 루틴 + 백업 루틴(선택)**. 백업은 더 가볍게 실행하는 대체안.
- 의무 인증 주기: **주6일(월~토)** 또는 **주7일(매일)** 중 선택.
- 마감: 해당일 **23:59**. 백업으로 실행해도 인증 인정(앱에서 '백업으로 인증' 표시).

### 2.2 결과형 (목표 달성) [MVP]
- **목표값을 미리 선언**하고 자율 실행 → **주기별로 결과 인증**.
- 목표 개수: 1개 이상 자유. 백업 없음.
- 의무 인증 주기: **매주 일요일 23:59**.
- 인증 시 **진행률/수치 입력** → 실행 여부 판단이 애매한 결과형의 약점 보완.
- 목표는 모호하지 않고 **구체적·측정 가능**하게(예: "누적 100km", "책 5권").

### 2.3 공통 규칙 [MVP]
- 루틴/목표를 정한 **이유를 함께 선언**(실행력 강화).
- 적립형·결과형 **둘 다 설정 가능**.
- **기간 내 루틴 변경 찬스 인당 1회**(변경 이력 기록).
- 타 소모임 과제와 연계되는 주제 금지(그룹 모드에서 반장 확인).

---

## 3. 앱 — 인증 · 알림 · 공유 · 기록

### 3.1 인증 [MVP]
- 사진 촬영/선택 + 메모(결과형은 수치)로 인증.
- **사진에 날짜·시각 자동 워터마크** — "사진에 날짜 필수 표기" 규칙을 앱이 자동 처리.
- 인증 시각은 **기기 시각으로 서버 없이 기록**(조작 방지, "올린 시간 기준" 자동화).
- 마감: 유형별 23:59.

### 3.2 알림 — 마감 3시간 전 (강제 완료 장치) [MVP]
- 미인증 시 **마감 3시간 / 1시간 / 30분 전** 로컬 알림.
- **알람으로 동작** (2026-08-07 강화): 앱 내장 알람음(`res/raw/alarm_beep.wav`)이
  **알람 볼륨**으로 재생되고, `FLAG_INSISTENT`로 **소리가 계속 반복**된다.
  `timeoutAfter=1분`이라 1분 뒤에는 시스템이 알림을 거둬 저절로 멈추고,
  그전에 **[알람 끄기]를 누르면 즉시 멈춘다**. 화면이 꺼져 있어도 `fullScreenIntent`로 크게 뜬다.
  예약은 `AndroidScheduleMode.alarmClock`(setAlarmClock) — 절전/도즈에서도 가장 확실히 울린다.
  알림 채널은 소리 설정이 굳어버리므로 특성을 바꾸면 **ID를 올린다**(현재 `habit_alarm_v3`,
  옛 채널은 시작 시 삭제). 기기 기본 알림음이 '없음'이어도 울리도록 소리를 앱이 들고 있는 게 핵심.
- 인증을 마치면 **그날 알림 자동 취소**.

> ### ⚠️ 알람 건드릴 때 반드시 지킬 것 (소리 안 나는 사고 2회 발생 후 정립)
> 1. **소리·중요도·진동을 바꾸면 채널 ID를 올린다.** 안드로이드 알림 채널은 한 번 생성되면
>    이 속성들을 코드로 바꿀 수 없다. ID를 그대로 두면 **코드를 아무리 고쳐도 반영되지 않는다.**
>    (현재 `habit_alarm_v4`, 옛 ID는 `_oldChannelIds`에 넣어 시작 시 삭제)
> 2. **알림 예외를 삼키지 않는다.** 예약 실패를 조용히 넘기면 "버튼을 눌러도 아무 일이 없는"
>    상태가 되어 원인 추적이 몇 배로 든다. 실패 사유는 화면(스낵바)에 그대로 띄운다.
> 3. **소리는 기기의 시스템 알람음**(`content://settings/system/alarm_alert`)을 쓴다.
>    앱에 음원을 번들하는 방식은 이 프로젝트에서 리소스가 APK에 병합되지 않아 실패했다(아래 참고).
> 4. **`reconcile()`은 `cancelAll()`을 쓰지 않는다.** 마감 알림 슬롯(1~`_maxPending`)만 지운다.
>    전체 취소를 하면 타이머 완료 알림·알람 테스트 예약까지 함께 사라진다.
> 5. **변경 후에는 실기기 확인이 필수.** 앱 → 🔔 → '알람 테스트(10초 뒤)' → 홈으로 나가서 대기.
>    알림 설정 화면 맨 위 **자가진단 카드**(권한·예약 건수·알람음 상태)로 1차 점검할 수 있다.
>
> **미해결 이슈**: `android/app/src/main/res/` 아래 **새로 만든 폴더**(`raw/`, `drawable-nodpi/`)의
> 파일이 APK에 병합되지 않는다. `flutter clean` · `android/.gradle` 삭제 · `build/` 삭제 후에도 재현.
> 기존 폴더(`mipmap-*`, `drawable`)의 리소스는 정상. 그래서 알람음은 시스템 URI로,
> 알림 큰 아이콘은 **Flutter 에셋을 파일로 복사해 `FilePathAndroidBitmap`으로** 우회했다.
> 네이티브 리소스가 꼭 필요해지면 이 문제부터 규명할 것.
- 적립형 주6일 선택자는 **일요일 알림 없음**. 결과형은 일요일에만.
- 이 기능이 앱의 존재 이유. **네이티브(설치형)라 앱이 닫혀 있어도 알림이 울린다**(PWA/웹은 불가).

### 3.3 공유 [MVP]
- 인증 직후·기록 앨범에서 **OS 공유 시트**로 카카오톡 오픈채팅방 등에 직접 전송.
- 앱이 카톡방에 자동 전송하는 것은 정책상 불가 → **사용자가 방을 골라 전송**(합법·안전).

### 3.4 기록 · 추억 [MVP]
- 인증 사진이 **타임라인 앨범**으로 쌓인다.
- [선택] 시즌 종료 시 **회고 카드**(총 인증 n회 / 달성률 %) 자동 생성.

### 3.5 (선택/향후) 그룹 모드 — 원래 모임 운영 요소
혼자 모드가 1순위지만, 원형이었던 다인 운영 규칙을 옵션으로 남겨둔다.
- **반장(운영자)**: 인증은 하되 체크·상금·벌금 대상 제외. [향후]
- **벌금/상금**: 미인증 카운트 기반, 인당 최대 5만원 미만, 반장 제외. [향후]
- **구글시트 자동 기록**: 반장 수기 기록을 앱이 대체. [향후]
- **기간(시즌)**: 예) 2025-07-13 ~ 09-13, 9주/63일. [선택] 시즌 설정으로 반복 운영.
> 그룹 모드는 인원 간 동기화가 필요해 **백엔드 도입 시점**에 함께 설계.

---

## 3.6 브랜드 — 바브바브(VAVEVAVE) 캐릭터 [적용 2026-08-07]

앱의 얼굴을 마스코트 **바브바브**(흰 털 원숭이, 네온 블루 액센트)로 통일했다.

- 원본: `etc/바브바브01.png`(Lv1 후드) — 이 한 장에서 모든 에셋을 뽑는다.
  `dart run tool/gen_character_assets.dart` → `assets/character/vave_face.png`(얼굴 원형 컷아웃),
  `vave_full.png`(전신), `assets/icon/*`(앱 아이콘). 좌표·컷아웃 규칙은 스크립트 상수로 조정.
- **도장 = 바브바브 얼굴**. `StampMark` / `StampButton` / `AppLogo`가 모두 얼굴 에셋을 쓴다.
- 팔레트도 캐릭터에서 뽑았다 — 네온 블루(브랜드) · **바이올렛(도장/성취)** · 딥 네이비(다크 배경).
  라이트/다크 양쪽 모두 캐릭터 톤으로 재설계(`lib/theme.dart`).
- **3초 인트로**(`lib/screens/splash_screen.dart`): 바브바브가 튀어오르고 도장 3개가 순차로
  '쾅' 찍히는 애니메이션. 이탈률 방어용 첫인상. 탭하면 즉시 건너뛴다.
- 앱 아이콘 갱신: `dart run flutter_launcher_icons` (웹은 `dart run tool/gen_web_icons.dart`).
- **도장 레벨 시스템** (2026-08-12): 루틴별 인증 일수로 도장이 진화 — 7일=2단계(탐구 바브·바이올렛→블루 링),
  21일=3단계(마스터 바브·골드 링). `AppState.levelFor()` 기준. 레벨 얼굴은 `etc/바브바브02·03.png`에서
  생성(`vave_face2/3.png`). 2단계부터 홈 카드에 그라데이션 테두리+레벨 배지, 좌측 아이콘·도장 달력도 진화.
- **루틴 아이콘 썸네일 선택기** (2026-08-12): 홈 카드 좌측 아이콘 탭 → 인증 사진/갤러리에서 선택 →
  핀치 확대·이동으로 정사각 크롭(`IconCropScreen`, RepaintBoundary 512px 캡처) → `Routine.iconPath`에 저장.
  백업 ZIP에 아이콘 사진 포함. 코드: `lib/widgets/routine_icon.dart`.

## 4. 앱 — 기술 & 현재 상태

- 스택: **Flutter 3.22+** (네이티브 Android/iOS), Material 3.
- 저장: 기기 로컬(shared_preferences + 앱 문서 폴더). 서버·계정 없음.
- 알림: flutter_local_notifications + timezone. 사진: image_picker + image(워터마크). 공유: share_plus.
- **현재 구현 완료(MVP 골격)**: 루틴 선언(적립/결과, 추천 루틴 프리셋), 사진 인증+워터마크, 마감 전 로컬 알림(인증 시 자동 취소), 공유, 기록 앨범, 오늘의 명언(내장 JSON + 웹 관리자 등록분 12시간마다 병합), 나눔고딕 테마 + 도장 아이덴티티 디자인, 앱 아이콘.
- 패키지명 확정: **com.keywordream.keepup** (2026-07-13, 실기기 SM-F766N 설치 확인)
- **웹(PWA) 지원 추가** (2026-08-06): 같은 코드베이스로 `flutter build web` → `log.keywordream.com/app/`.
  아이폰 등 설치 없이 브라우저에서 사용. 자세한 범위·제약은 4.2 참고.
- **다음 개발 후보**: 시즌 기간 설정 + 회고 카드, 달성률/미인증 통계, 루틴 변경 UI(로직 존재), 알림 시각 커스터마이즈, [임시] 테스트 알림 버튼 제거.

### 4.1 검증(인증) 수단 로드맵 — "스마트폰으로 모든 검증"
> 핵심 철학: KeepUp은 **실행을 검증하는 앱**. 원조 챌린지 회원들의 실제 루틴을 기준으로 설계.

| 루틴 예시 | 현재 검증 | 향후 검증 |
|---|---|---|
| 걷기(5천/6천/1만보) | 만보기 스크린샷 사진 인증 | Health Connect 걸음수 자동 검증 |
| 팔굽혀펴기/계단 15층/근력 | 사진(+메모) | 타이머·영상 인증 |
| 독서 10~20분/1시간+요약 | 책 사진 + 메모(요약) | 앱 내 타이머 인증 |
| 명상 15분 | 사진 | **앱 내 타이머 인증**(n분 이상 측정) |
| 중국어 발음연습(매일 한마디) | 사진 | **녹음 인증**(마이크 녹음 첨부) — 발음 연습 이력 |
| 문장 50번 쓰기/유튜브 정리 | 사진 + 메모 | — |
| 온라인몰 상품 등록/주식 공부 | 스크린샷 인증 | — |
| 일찍 일어나기/출근 기도 | 사진(워터마크 시각이 증명) | **시간대 제한 옵션**(예: 오전 7시 전 인증만 인정) |
| 건강식품 챙겨먹기 | 사진 | — |

구현 완료(2026-07-14): **검증 전체 완성 — 사진 / 타이머 / 녹음 / 동영상 / 걸음수(Health Connect 자동) + 시간대 제한 + 소감 필수** + 도장 달력 + 달성률 %.
기간 정책: **본인 설정** (최소 30일, 프리셋 30일/63일/100일/1년/2년, 완료일 변경 가능) — "1년, 2년 쌓일 때 진짜 습관".
기술 노트: health 13.3.1(minSdk 26 요구 → 26으로 상향), share_plus는 health와 win32 충돌로 12.x 고정. 릴리스 APK: `E:\app\KeepUp-release.apk` (새 기기 설치용, debug 서명).

### 4.2 웹(PWA) — 아이폰용 보조 창구 [구현 완료 2026-08-06]
> 목적: **아이폰 사용자가 설치 없이 바로** 쓰게 하는 것. 설치형 앱이 여전히 주인공.

- 주소: `https://log.keywordream.com/app/` (홍보 사이트 Worker의 정적 자산으로 함께 배포).
- 되는 것: 루틴 선언 / **사진 인증(날짜 워터마크)** / **타이머 인증** / URL 인증 / 소감·진행수치 /
  도장 달력 · 달성률 / 기록 앨범 / 회고 카드 / 공유 / **백업 ZIP 내보내기·불러오기(앱 ↔ 웹 데이터 이사)**.
- 안 되는 것(브라우저 한계): **마감 알림 전부**(예약형 로컬 알림 없음), 녹음·동영상 인증,
  걸음수 자동 검증(Health Connect), 갤러리 자동 저장, 웹 계정 로그인 — 화면에서 자동으로 숨기거나
  "앱에서만 지원" 안내 + 사진 인증으로 대체.
- 저장 위치: 루틴·인증 기록은 브라우저 localStorage, **사진은 IndexedDB**(`MediaStore`).
  → **폰 앱과 데이터가 분리**되고, 브라우저 데이터를 지우면 사라진다. 이사는 백업 ZIP으로.
- 구조: 플랫폼 분기는 `lib/services/media_store.dart`(io/web 조건부 import) + `file_saver.dart`,
  화면은 `widgets/cert_photo.dart`로 사진을 그린다. 나머지는 `kIsWeb` 가드.
- 빌드·배포: `powershell -File tool\build_web.ps1` (아이콘 변경 시 `dart run tool/gen_web_icons.dart` 먼저)
  → `cd E:\app\keywordream_web\keepup ; npm run deploy`.
- 웹 전용 경로 테스트: `flutter test --platform chrome test/web_media_store_test.dart`.

---

## 5. 웹 — 홍보 & 성과 & 회원 (Cloudflare Workers)

무료 티어 기반: **Workers(정적 자산 + API) + D1(SQLite) + KV(세션) + R2(이미지)**.
R2는 결제수단 등록이 필요하지만 10GB까지 무료. 저장 용량이 커지면 자체 서버로 이관 검토.
구현은 exansys.net(betona1/exansys.net)에서 검증된 스택을 포팅: **React 19 + Vite + Hono + Drizzle**.

### 5.0 아키텍처 [MVP]
- 폴더: `E:\app\keywordream_web\main`(keywordream.com), `E:\app\keywordream_web\keepup`(keepup.keywordream.com) — 각각 독립 Worker.
- **공유 자원**: D1 `keywordream-db`(회원+게시판, 마이그레이션은 main에서만 관리), KV `SESSIONS`, R2 `keepup-media`.
- **SSO**: OAuth/이메일 로그인은 **keywordream.com에서만** 처리. 세션 쿠키를 `Domain=.keywordream.com`으로 발급 → keepup 서브도메인 Worker가 같은 KV를 읽어 로그인 인식.
- keepup에서 로그인 필요 시 `keywordream.com/login?next=<돌아올 URL>`로 이동, 로그인 후 복귀(next는 *.keywordream.com만 허용).

### 5.1 keywordream.com — 브랜드 메인 + 회원 [MVP]
- **홈**: 스튜디오/브랜드 소개 + 제품(KeepUp) 카드 → keepup 사이트로 연결.
- **간편로그인**(`/login`): 구글 / 네이버 / 카카오 OAuth + 이메일 6자리 인증코드(패스워드리스, Resend 발송). 비밀번호 저장 안 함.
- **회원관리**: 마이페이지(`/me` — 이름 변경, 로그아웃, 탈퇴), 관리자(`/admin` — 회원 목록·역할 변경). 역할: member / admin. `ADMIN_EMAIL`과 일치하는 계정은 자동 admin.
- 스팸 방지: Cloudflare Turnstile(이메일 코드 발송 폼).

### 5.2 keepup.keywordream.com — 앱 컴패니언 [MVP]
- **홈**: 앱 소개 + 다운로드(현재 "출시 예정") + 도장 스탬프 시그니처.
- **성과 인증 게시판**(`/stories`): 실제 게시판(D1+R2). 목록/상세/글쓰기(제목·기간·루틴·본문·사진 업로드)/댓글/응원(스탬프 찍기). 열람은 누구나, 작성·댓글·응원은 로그인 필요(SSO).
- **법적**: 개인정보처리방침(`/privacy`), 이용약관(`/terms`) — 앱은 수집 없음 + 웹 회원정보 수집 항목 명시. 값 채우고 검토 필요.
- **문의**(`/support`): 이메일 + FAQ.
- **딥링크 파일**(`.well-known/`): iOS AASA, Android assetlinks — 앱 ID 확정 시 값 채움.

### 5.3 현재 인프라 상태
- 도메인 keywordream.com **구입 완료**, Cloudflare 호스팅/네임서버 **미설정**.
- 셋업 절차는 `E:\app\keywordream_web\SETUP.md` 참고(네임서버 → D1/KV/R2 생성 → OAuth 앱 등록 → 시크릿 → 배포).

---

## 6. 앱 ↔ 웹 연동

- **웹 → 앱**: 다운로드 버튼(스토어), 딥링크(링크 누르면 앱 열기). [MVP 골격 / 값은 향후]
- **앱 → 웹**: 앱 내 '개인정보처리방침·이용약관·문의' 링크가 웹을 가리킴. [MVP]
- **성과 공유**: 앱의 완주 기록(사진·통계)을 웹 성과 게시판에 올리는 흐름. 게시판은 이미 존재하므로, 앱에서 '웹에 자랑하기' → 브라우저로 `/stories/new` 열기(사진은 공유 시트/수동 첨부)부터 시작, 추후 업로드 API 직접 연동. [2단계]
- 딥링크 준비물: iOS 팀ID·번들ID(예: com.keywordream.keepup), Android 패키지명·서명 SHA256.

---

## 7. 배포 & 운영

- **앱**: Google Play(주력) 심사·출시 → 이후 App Store. 개인정보처리방침 URL(keepup.keywordream.com/privacy) 사용.
- **웹앱(PWA)**: `log.keywordream.com/app/` — 아이폰 임시 지원용 보조 수단(알림 없음). 4.2 참고.
- **웹**: Cloudflare Workers 2개(`keywordream-main`, `keepup-site`) + 커스텀 도메인. 각 폴더에서 `npm run deploy`.
- 저장소: 앱(habit_challenge)과 웹(keywordream_web)은 **별도 폴더/저장소**로 유지.

---

## 8. 로드맵

**1단계 — 지금 (진행 중)**
- 웹 2종 구축: 메인(랜딩+로그인+회원관리) + keepup(홍보+성과 게시판). ← 코드 작성 완료 후 Cloudflare 셋업·배포.
- 도메인 네임서버 Cloudflare 연결, D1/KV/R2 생성, OAuth 앱(구글/네이버/카카오) 등록.

**2단계 — 앱 MVP 완성 & 출시 준비**
- 앱 빌드·실기기 테스트, 알림 안정화.
- 시즌 기간 설정 + 회고 카드, 달성률 통계, 루틴 변경 UI.
- 앱 아이콘·이름·스플래시, 스토어 등록, 딥링크 값 채우기.
- 앱 → 웹 게시판 '자랑하기' 연결.

**3단계 — 확장**
- [x] Flutter Web PWA(아이폰 임시 지원) — 2026-08-06 완료. 다음은 App Store 출시.
- 이미지 저장 자체 서버 이관 검토(R2 용량 초과 시).
- [선택] 그룹 모드(반장·벌금·동기화) 재도입 검토.

---


## 9. 확정된 사항 (2026-07-13)

- [x] 간편로그인: 구글 + 네이버 + 카카오 + 이메일(6자리 인증코드) 전부 지원
- [x] 인증 구현: exansys.net 스택 포팅(Workers+Hono+D1+KV), 필요 시 OAuth 콘솔에 앱 추가 등록
- [x] 성과 게시판: 지금부터 실제 게시판으로 구축(D1+R2)
- [x] 앱-웹 계정: 앱은 계정 없음, 게시판에 올릴 때만 웹 로그인
- [x] 앱 우선순위: 안드로이드 주력 → PWA(아이폰 임시) → App Store

## 10. 미결정 사항

- [ ] 앱 번들ID/패키지명 확정 (딥링크·스토어에 필요, 예: com.keywordream.keepup)
- [ ] 시즌 기본 기간 고정 vs 사용자 설정
- [ ] 혼자 모드에서 '자율 벌금'을 재미 요소로 남길지
- [ ] 성과 게시판 공개 범위(현재: 열람 전체 공개 / 작성 로그인)
- [ ] 법적 페이지 연락처·시행일, 분석/크래시 도구 도입 여부
- [ ] 알림 반복 간격·문구 커스터마이즈 범위
- [ ] 이메일 로그인 발송 도메인(Resend에 keywordream.com 등록 필요)
